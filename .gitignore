# --- VAYU ---
.env
data/cache/
# Dossiers are generated at dispatch time; curated samples live in docs/samples/.
data/dossiers/
data/vayu.duckdb
data/vayu.duckdb.wal
data/vayu_demo.duckdb
data/vayu_demo.duckdb.wal

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# Node / Next
node_modules/
.next/
out/
.turbo/
npm-debug.log*
.DS_Store
*.tsbuildinfo

# Notebooks
.ipynb_checkpoints/

data/vayu_deploy.duckdb

# GCP/GEE service-account keys — never committed
secrets/

# Claude Code local permission settings — machine-specific trust, not shared
.claude/settings.local.json

# Stale hackathon-branded artifacts kept locally, never committed or deployed
_archive_not_in_repo/
