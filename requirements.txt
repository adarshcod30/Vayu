# VAYU API + pipeline + core science
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
certifi>=2024.2.2
# file uploads (citizen photo submission)
python-multipart>=0.0.9
duckdb>=1.0
pandas>=2.2
pyarrow>=15.0
pytz>=2024.1
numpy>=1.26
shapely>=2.0
loguru>=0.7
python-dotenv>=1.0
apscheduler>=3.10

# Phase 2+ (declared now so `make seed` env is stable across phases)
lightgbm>=4.3
scikit-learn>=1.4
holidays>=0.50
shap>=0.45
matplotlib>=3.8

# dev
pytest>=8.0
pytest-cov>=5.0
# Fails a hung test loudly with a stack trace instead of blocking forever —
# added after a real hang (an unbounded numpy broadcast, and separately a
# macOS OpenMP thread-pool conflict between torch and scikit-learn) sat
# silently for hours with no output before being tracked down.
pytest-timeout>=2.3
reportlab==5.0.0
pymupdf==1.28.0
ruff>=0.6
h3>=4.0
boto3>=1.34
rasterio>=1.3

# Objective-1's CNN-LSTM (vayu_core/national/surface_aqi.py) needs torch, but
# it is deliberately NOT listed here: it is a training/offline-scoring-only
# dependency, never imported by the API request path, and the deployed
# container has no reason to carry its weight. Install separately for local
# training: pip install torch --index-url https://download.pytorch.org/whl/cpu
